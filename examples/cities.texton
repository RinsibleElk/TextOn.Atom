#include "gender.texton"

@var @free $Country = "Which country are you writing about?"
  {
    "U.K."
    "Germany"
  }
@var @free $City = "Which city are you writing about?"
  {
    "London" [$Country = "U.K."]
    "Berlin" [$Country = "Germany" && %Gender = "Male"]
    "Paris" [$Country <> "Germany" && $Country <> "U.K."]
  }
